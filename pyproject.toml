[project]
name = "fine-tuning-llama3-2"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "accelerate>=1.0.1",
    "datasets>=3.0.1",
    "huggingface-hub>=0.25.2",
    "torch>=2.5.0",
    "transformers>=4.45.2",
]
